# CDN origin used only on the server (proxied — never exposed to the player).
NEXT_PUBLIC_BUNNY_CDN_URL=https://filmflik.b-cdn.net

# Pull Zone → Security → URL Token Authentication Key (server-only)
BUNNY_TOKEN_KEY=
BUNNY_TOKEN_AUTH_MODE=sha256
BUNNY_TOKEN_TTL=7200
